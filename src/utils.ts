/**
 * Suma dos números me da igual
 * @param a 
 * @param b 
 * @returns "La suma de los dos"
 */

 export const suma = (a:number, b:number): number => {
    return a+b}

export const duplicar = (a:number): number => {
   return a*2
}


